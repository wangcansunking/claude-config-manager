import { Text } from 'ink';
import type { CcmStore, StoreState } from '../../store.js';

export function Commands(_props: { state: StoreState; store: CcmStore }) {
  return <Text dimColor>(commands TODO)</Text>;
}

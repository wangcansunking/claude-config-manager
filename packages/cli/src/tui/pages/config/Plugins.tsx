import { Text } from 'ink';
import type { CcmStore, StoreState } from '../../store.js';

export function Plugins(_props: { state: StoreState; store: CcmStore }) {
  return <Text dimColor>(plugins TODO)</Text>;
}

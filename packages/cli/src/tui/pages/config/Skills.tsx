import { Text } from 'ink';
import type { CcmStore, StoreState } from '../../store.js';

export function Skills(_props: { state: StoreState; store: CcmStore }) {
  return <Text dimColor>(skills TODO)</Text>;
}

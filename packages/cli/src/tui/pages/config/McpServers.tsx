import { Text } from 'ink';
import type { CcmStore, StoreState } from '../../store.js';

export function McpServers(_props: { state: StoreState; store: CcmStore }) {
  return <Text dimColor>(mcpServers TODO)</Text>;
}

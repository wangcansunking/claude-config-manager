import { Text } from 'ink';
import type { CcmStore, StoreState } from '../../store.js';

export function Settings(_props: { state: StoreState; store: CcmStore }) {
  return <Text dimColor>(settings TODO)</Text>;
}
